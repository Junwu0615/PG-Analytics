name: Platform Genesis Universe Analytics
on:
  workflow_dispatch:
  schedule:
    # Every day 00:10 (UTC+8)
    - cron: "10 16 * * *"
  push:
    branches:
      - main

permissions:
  contents: write
#  workflows: write

jobs:
  analytics:
    runs-on: ubuntu-latest
    env:
      ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Collect Repository Metrics
        env:
          PG_TOKEN: ${{ secrets.PG_TOKEN }}
        run: |
          python scripts/collect.py

      - name: Export History
        run: |
          python scripts/export_history.py

      - name: Generate Reports
        run: |
          python scripts/generate_report.py

      - name: Sync README
        run: |
          python scripts/sync_readme.py

      - name: Commit Analytics
        env:
          PG_TOKEN: ${{ secrets.PG_TOKEN }}
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          
          git remote set-url origin https://x-access-token:${PG_TOKEN}@github.com/${{ github.repository }}.git
          
          git add .
          git diff --cached --quiet || git commit -m "chore(analytics): update repository metrics"
          git push